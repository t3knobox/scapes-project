runpod>=1.6
# Stable Audio Open runs via diffusers' StableAudioPipeline.
diffusers>=0.30
# Pinned to 4.x: transformers 5.x needs torch >=2.6; base image has torch 2.4.1.
# (transformers provides the T5 text encoder for SAO, and MusicGen as a fallback engine.)
transformers==4.44.2
tokenizers<0.20
huggingface_hub<0.26
accelerate>=0.33,<1.0
sentencepiece>=0.2
protobuf
lameenc>=1.7
numpy>=1.26,<2
# torch is provided by the RunPod base image
